template_type: simple

pattern: |
  {0:string:float} = {1:string:float} + {2:float}

code_translation: |
  mov rax, {2}
  movq xmm0, rax
  movq xmm1, qword [rel {1}]
  addss xmm1, xmm0
  movq [rel {0}], xmm1
